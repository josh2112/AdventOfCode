{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: cl.exe build active file",
            "dependsOn": "_makebuildfolder",
            "command": "cl.exe",
            "args": [
                "/Zi",
                "/EHsc",
                "/O2",
                "/nologo",
                "/std:c++20",
                "/Fo${fileDirname}\\build\\${fileBasenameNoExtension}.obj",
                "/Fe${fileDirname}\\build\\${fileBasenameNoExtension}.exe",
                "/Fd${fileDirname}\\build\\${fileBasenameNoExtension}.pdb",
                "${file}"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": ["$msCompile"],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        },
        {
            // Create build directory (if it doesn't already exist)
            "label": "_makebuildfolder",
            "type": "shell",
            "command": "md ${fileDirname}\\build -ea 0; echo $LASTEXITCODE",
            "problemMatcher": [],
            "group": "none"
        }
    ],
    "version": "2.0.0"
}
